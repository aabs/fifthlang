alias x as <http://example.org/>;

class Resource in <x:> {
    uri: string;
    value: int;
}

main(): int {
    // Build two graphs inline and merge them; avoid using variable declarations to bypass parser ambiguity
    std.print("DEBUG START");
    g1: graph = <x:s1, x:p, 42> + <x:s2, x:p, 43>;
    g2: graph = <x:s1, x:p, 42> + <x:s2, x:p, 43>;
    g3: graph = g1 + g2;
    std.print("TRIPLE_COUNT:");
    std.print(KG.CountTriples(g3));

    return 0;
}
