alias ex as <http://example.org/>;
testStore: store = sparql_store(<http://example.org/test>);

main(): int {
    g: graph = KG.CreateGraph();
    t: triple = <ex:s, ex:p, ex:o>;
    g += t;
    return 0;
}
