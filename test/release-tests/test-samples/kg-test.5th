store default = sparql_store(<http://example.org/test>);
alias ex as <http://example.org/>;

main(): int {
    g: graph = KG.CreateGraph();
    t: triple = <ex:s, ex:p, "test">;
    g += t;
    return 0;
}
