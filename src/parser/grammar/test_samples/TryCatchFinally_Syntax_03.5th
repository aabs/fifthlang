// Catch and inspect exception (filter simulated in body)
main(): int {
    try {
        doWork();
    } catch (ex: Exception) {
        if (ex.Message != null) {
            std.print(ex.Message);
        }
    } finally {
        std.print("Done");
    }
    return 0;
}

doWork(): void {
}
