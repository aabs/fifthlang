// Test: CONSTRUCT query with multiple lines
main(): int {
    q: Query = ?<
        CONSTRUCT {
            ?s ex:prop ?o
        }
        WHERE {
            ?s ?p ?o
        }
    >;
    return 0;
}
