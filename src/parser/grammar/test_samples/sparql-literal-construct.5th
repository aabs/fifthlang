// Test: CONSTRUCT query with multiple lines
main(): int {
    q: Query = ?<
        CONSTRUCT {
            ?s <http://example.org/prop> ?o
        }
        WHERE {
            ?s ?p ?o
        }
    >;
    return 0;
}
