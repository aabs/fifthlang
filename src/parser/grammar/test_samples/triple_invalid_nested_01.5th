// T005: invalid nested list (should produce TRPL006)
// intent: invalid — expect parse error
alias ex as <http://example.org/>;

main(): int {
	g: graph = <ex:s, ex:p, [[ex:o1, ex:o2], ex:o3]>; // expect TRPL006
	return 0;
}